# 数字生命 MVP 依赖 (参见 arch.md)
# 要求: Python 3.11（以下版本均在 3.11 下测试可用）

# 配置与环境
python-dotenv>=1.0.0
PyYAML>=6.0.1

# 异步与 HTTP
aiohttp>=3.9.0
aiofiles>=23.2.0

# 视觉 (截屏)
mss>=9.0.1
Pillow>=10.0.0

# 语音 (STT / VAD / TTS)
edge-tts>=6.1.0   # TTS，嘴巴播报「说的话」；config tts_voice
# webrtcvad 或 silero-vad

# 大脑 (Gemini)：主脑、STT、Mem0 均使用 google-genai
google-genai>=1.0.0

# 记忆 (Mem0，需 GEMINI_API_KEY)
mem0ai>=0.1.0

# Telegram Bot（独立入口 python -m src.telegram）
python-telegram-bot>=21.0

# Web (FastAPI，与 main 解耦；语音上传需 multipart)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.6

# 日志与工具
colorlog>=6.8.0
