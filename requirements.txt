# 数字生命 MVP 依赖 (参见 arch.md)
# 要求: Python 3.11（以下版本均在 3.11 下测试可用）

# 配置与环境
python-dotenv>=1.0.0
PyYAML>=6.0.1

# 异步与 HTTP
aiohttp>=3.9.0
aiofiles>=23.2.0

# 视觉 (截屏)
mss>=9.0.1
Pillow>=10.0.0

# 语音 (STT / VAD / TTS)：STT 使用 Gemini 多模态转写，与主脑共用 google-generativeai
# webrtcvad 或 silero-vad
# edge-tts 或 fish-speech 等

# 大脑 (Gemini)：主脑用 google-generativeai；Mem0 的 Gemini 用 google-genai
google-generativeai>=0.8.0
google-genai>=1.0.0

# 记忆 (Mem0)
# mem0ai 或 自建向量库 (qdrant-client, chromadb)

# VTube Studio
# pyvts

# Web (FastAPI，与 main 解耦；语音上传需 multipart)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.6

# 日志与工具
colorlog>=6.8.0
